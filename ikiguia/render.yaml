services:
  - type: web
    name: ikiguia
    env: python
    buildCommand: "pip install -r ikiguia/requirements.txt"
    startCommand: gunicorn ikiguia.wsgi:application
    envVars:
      - key: DJANGO_SETTINGS_MODULE
        value: ikiguia.settings
      - key: PYTHON_VERSION
        value: 3.10
